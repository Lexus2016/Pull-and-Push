"""Implement these functions so the hidden tests pass — EDIT ONLY THIS FILE.

A scoring harness runs a hidden pytest suite against your code and reports the
percentage of tests passing (target: 100). Make one focused improvement per step.
"""


def is_prime(n: int) -> bool:
    """Return True iff n is a prime number (n < 2 is not prime)."""
    raise NotImplementedError


def fib(n: int) -> int:
    """Return the n-th Fibonacci number, 0-indexed: fib(0)=0, fib(1)=1, fib(2)=1, ..."""
    raise NotImplementedError
