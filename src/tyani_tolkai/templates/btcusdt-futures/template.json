{
  "id": "btcusdt-futures",
  "name": "BTCUSDT 5m futures strategy",
  "keywords": ["trading", "btc", "btcusdt", "futures", "crypto", "binance", "strategy",
               "backtest", "sharpe", "drawdown", "leverage", "trade", "bot"],
  "summary": "Tune a leveraged long/short BTCUSDT 5m strategy. The executor edits strategy.py (signals() + PARAMS); a committed walk-forward backtest harness scores return, liquidations and drawdown on a held-out out-of-sample tail (anti-overfit) of real data.",
  "config": {
    "mode": "asymmetric",
    "agents": {"executor": {"engine": "claude", "timeout": 600},
               "validator": {"engine": "claude", "timeout": 300}},
    "roles": {
      "executor": {"task": "Edit strategy.py — tune PARAMS and/or rewrite the signals() logic (add indicators, change entry/exit rules, restructure). Do NOT modify or run anything under ../metrics/ (the scoring harness). One focused change per step."},
      "validator": {"goal": "Review the strategy and the change: your honest opinion on whether the approach is soundly built, why the score moved, and one or two concrete ideas (PARAMS or signals logic) to try next."}
    },
    "evaluation": {
      "adapter": "numeric",
      "command": "{python} ../metrics/run_backtest.py --strategy strategy.py --symbol BTCUSDT",
      "target_score": 90,
      "metrics": [
        {"name": "return_oos_pct", "dir": "higher", "weight": 0.5, "target": 100},
        {"name": "liquidations", "dir": "lower", "weight": 0.25, "target": 0},
        {"name": "max_drawdown_pct", "dir": "lower", "weight": 0.2, "target": 30},
        {"name": "max_drawdown_days", "dir": "lower", "weight": 0.15, "target": 5}
      ]
    },
    "limits": {"max_iterations": 40, "plateau_N": 8, "step_seconds": 600},
    "sandbox": {"backend": "local"}
  }
}
